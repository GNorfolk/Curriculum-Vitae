{
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "token": "{{user `token`}}",
      "region": "eu-west-1",
      "instance_type": "t3.micro",
      "ssh_username": "ec2-user",
      "ami_name": "curriculumvitae-{{timestamp}}",
      "vpc_filter": {
        "filters": {
          "tag:Name": "main"
        }
      },
      "subnet_filter": {
        "filters": {
          "tag:Name": "elb_az_a"
        }
      },
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "amzn2-ami-hvm-*",
          "root-device-type": "ebs"
        },
        "owners": ["amazon"],
        "most_recent": true
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": "ls"
    }
  ]
}
