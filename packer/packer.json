{
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `access_key`}}",
      "secret_key": "{{user `secret_key`}}",
      "token": "{{user `token`}}",
      "region": "eu-west-1",
      "instance_type": "t3.micro",
      "ssh_username": "ec2-user",
      "ami_name": "packer-cv-{{timestamp}}",
      "source_ami": "ami-01f14919ba412de34"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": "ls"
    }
  ]
}
